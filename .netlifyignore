# Dependencies and secrets are not published
node_modules/
.env
.env.*

# SQLite admin database is local/server-only (not used on static Netlify deploy)
data/*.db
data/*.db-journal

# Dev / tooling
scripts/
*.log
cookies.txt
.git/
