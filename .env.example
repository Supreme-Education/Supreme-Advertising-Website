# Copy to .env and set your admin password before first run.
# No DATABASE_URL — this project uses SQLite (data/admin.db) via better-sqlite3, not Prisma.
ADMIN_PASSWORD=change-me-to-a-strong-password
SESSION_SECRET=change-me-to-a-long-random-string
PORT=3000
